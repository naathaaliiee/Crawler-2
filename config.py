# Crawler Configuration

# Starting URL for the crawler
START_URL = "https://example.com"

# Maximum depth to crawl (how many links deep)
MAX_DEPTH = 2

# Maximum number of pages to crawl
MAX_PAGES = 50

# Delay between requests (seconds) - be polite to servers
DELAY = 1.0

# Allowed domains (leave empty to allow only the start URL domain)
# Example: ["example.com", "www.example.com"]
ALLOWED_DOMAINS = []

# User agent string
USER_AGENT = "Mozilla/5.0 (compatible; WebCrawler/1.0)"

# Request timeout (seconds)
TIMEOUT = 10
